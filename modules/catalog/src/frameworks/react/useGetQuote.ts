export {};

// TODO: or GetQuoteReactInteractor (that includes both Controller + Presenter) or GetQuoteReactPresenter (that includes only Presenter for more decupling (but not really needed in front-end world where the read and write model are coupled by nature)).
